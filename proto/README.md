# Proto Directory

This directory is reserved for Protocol Buffer definitions (future use).

Currently, the system uses JSON over TCP for RPC communication.

## Future Proto Files

- `lock.proto` - Lock service definitions
- `leader.proto` - Leader election definitions
- `cluster.proto` - Cluster management definitions
